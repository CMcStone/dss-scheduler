<!DOCTYPE html>
<html lang="en">
<head>
  <title>DSS Scheduler</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	
	<script id="tmpl-question" type="text/template">
	  	<p>{{= question.question }}</p><input type="text" />
	</script>
  
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a class="brand" href="#" style="color: #fff;">DSS Scheduler</a>
        <div class="nav-collapse">
          <ul class="nav">
            <% unless session[:cas_user].nil? %>
            <%= li_link_with_class({:title => 'New Request', :controller => 'reservations', :action => 'new'}) %>
            <%= li_link_with_class({:title => 'Existing Requests', :controller => 'reservations', :action => 'index'}) %>
			<%= li_link_with_class({:title => 'Admin', :controller => 'admin', :action => 'index'}) %>
            <% end %>
          </ul>
          
          <ul class="nav pull-right">
            <% unless session[:cas_user].nil? %>
            <li class="dropdown">
              <%= li_link_with_class({:title => 'Log out', :controller => 'application', :action => 'logout'}) %>
            </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div> <!-- .navbar -->

  <div class="container" style="margin-top: 65px;">
    <div class="row">
      <div class="span12">
        <%= yield %>
      </div>
    </div>
    
  </div> <!-- .container -->

</body>
</html>
